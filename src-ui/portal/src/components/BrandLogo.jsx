export default function BrandLogo({ className='' }) {
    return (
        <div className={`font-extrabold tracking-[0.2em] text-2xl text-black drop-shadow-logo ${className}`}>
            CARBIRD
        </div>
    )
}
